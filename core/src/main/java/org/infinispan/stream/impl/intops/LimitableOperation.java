package org.infinispan.stream.impl.intops;

/**
 * This is a placeholder interface that is used to signify that a limit operation applied after this can be done
 * on both the remote and local node to reduce memory consumption greatly.
 */
public interface LimitableOperation {
}
